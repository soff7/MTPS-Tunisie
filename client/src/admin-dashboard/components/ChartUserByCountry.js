// src/admin-dashboard/components/ChartUserByCountry.js
import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const ChartUserByCountry = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">
          Graphique temporairement indisponible
        </Typography>
        <Typography variant="body2">
          Le composant graphique est en cours de maintenance.
        </Typography>
      </CardContent>
    </Card>
  );
};

export default ChartUserByCountry;